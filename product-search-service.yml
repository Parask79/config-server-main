
spring:
  data:
    mongodb:
      uri: ${DB_HOST:mongodb://paras:admin@ec2-13-235-247-183.ap-south-1.compute.amazonaws.com/test}
      auto-index-creation: true
  kafka:
    bootstrap-servers: ${ZOOKEEPER_HOST:localhost:9092}


elastic:
  search:
    host-name: ${ELASTIC_SEARCH_HOST:localhost:9200}
